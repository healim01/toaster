import { ToastFrame } from '@/components/preview';

const PreviewSection = () => {
  return (
    <section className="h-[800px] md:h-full w-[300px] flex flex-col justify-center items-center">
      <ToastFrame />
    </section>
  );
};

export default PreviewSection;
