import {
  blackFrame,
  blueFrame,
  butterFrame,
  pinkFrame,
  whiteFrame,
} from '@/assets/frames';

export const basicFrames: Record<string, string> = {
  블랙: blackFrame,
  화이트: whiteFrame,
  블루베리: blueFrame,
  핑크: pinkFrame,
  버터: butterFrame,
};
