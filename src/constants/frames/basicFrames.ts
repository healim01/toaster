import {
  blueFrame,
  butterFrame,
  frame,
  pinkFrame,
  whiteFrame,
} from '@/assets/frames';

export const basicFrames: Record<string, string> = {
  블랙: frame,
  블루베리: blueFrame,
  핑크: pinkFrame,
  버터: butterFrame,
  화이트: whiteFrame,
};
