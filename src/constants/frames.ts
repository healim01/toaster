import {
  frame,
  gdyFrame,
  grassFrame,
  greenFrame,
  pinkGreenFrame,
} from '@/assets/frames';

export const frames: Record<string, string> = {
  default: frame,
  버건디: gdyFrame,
  무한도전: greenFrame,
  핑크그린: pinkGreenFrame,
  초록초록: grassFrame,
};
