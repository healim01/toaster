import {
  blueFrame,
  butterFrame,
  frame,
  gdyFrame,
  grassFrame,
  greenFrame,
  maxFrame,
  pinkFrame,
  whiteFrame,
} from '@/assets/frames';

export const frames: Record<string, string> = {
  default: frame,
  버건디: gdyFrame,
  무한도전: greenFrame,
  초록초록: grassFrame,
  블루베리: blueFrame,
  핑크: pinkFrame,
  버터: butterFrame,
  화이트: whiteFrame,
  맥스날드: maxFrame,
};
